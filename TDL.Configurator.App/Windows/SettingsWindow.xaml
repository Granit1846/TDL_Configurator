<Window x:Class="TDL.Configurator.App.Windows.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Настройки" Width="700" Height="220"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Путь к игре (корень Skyrim Special Edition)" FontWeight="SemiBold"/>

        <Grid Grid.Row="1" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="GamePathBox" Height="28" Margin="0,0,8,0"/>
            <Button Grid.Column="1" Width="120" Height="28" Content="Выбрать..." Click="Browse_Click"/>
        </Grid>

        <TextBlock Grid.Row="2" Margin="0,10,0,0" Opacity="0.7"
                   Text="Пример: F:\SteamLibrary\steamapps\common\Skyrim Special Edition"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Width="110" Margin="0,0,8,0" Content="Сохранить" Click="Save_Click"/>
            <Button Width="110" Content="Отмена" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
